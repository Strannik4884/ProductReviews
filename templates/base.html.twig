<!DOCTYPE html>
<html lang="ru">
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1.0"/>
    <link rel="icon" type="image/png" href="./images/favicon.png">
    <title>{% block title %}Отзовик{% endblock %}</title>
    {% block stylesheets %}
    <!-- Fonts -->
    <link href="./fonts/Mazzard_M_Light/MazzardSoftM-Light.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <!-- CSS -->
    <link type="text/css" rel="stylesheet" href="./css/materialize.min.css" media="screen,projection"/>
    <link type="text/css" rel="stylesheet" href="./css/materialert.css" media="screen,projection"/>
    <link type="text/css" rel="stylesheet" href="./css/style.css"/>
    {% endblock %}
</head>
<body>
<!-- Main fixed menu -->
<div class="navbar-fixed">
    <nav>
        <div class="nav-wrapper teal lighten-1" role="navigation">
            <div class="container">
                <!-- Site logo -->
                <a href="{{ path('app_home') }}" class="brand-logo"><span class="logo-text">Отзовик</span></a>
                <!-- Site menu -->
                <ul class="right hide-on-med-and-down">
                    {% if app.user %}
                        <li><a class="nav-text" href="{{ path('app_profile') }}">Мой профиль</a></li>
                    {% endif %}
                    <li><a class="nav-text" href="{{ path('app_about') }}">О проекте</a></li>
                    {% if app.user is same as(null) %}
                        <li><a class="nav-text" href="{{ path('app_register') }}">Регистрация</a></li>
                        <li><a class="nav-text" href="{{ path('app_login') }}">Вход</a></li>
                    {% endif %}
                    {% if app.user %}
                        <li><a class="nav-text" href="{{ path('app_logout') }}">Выход</a></li>
                    {% endif %}
                </ul>
                <!-- Mobile hamburger menu icon -->
                <a href="#" data-target="nav-mobile" class="sidenav-trigger"><i class="material-icons">menu</i></a>
            </div>
        </div>
    </nav>
</div>
<!-- Mobile hamburger menu -->
<ul id="nav-mobile" class="sidenav">
    <li><a href="{{ path('app_home') }}">Главная</a></li>
    <li><a href="{{ path('app_profile') }}">Мой профиль</a></li>
    <li><a href="{{ path('app_about') }}">О проекте</a></li>
    <li><a href="{{ path('app_register') }}">Регистрация</a></li>
    <li><a href="{{ path('app_login') }}">Вход</a></li>
</ul>
<!-- Site content -->
<main>
    <div class="container">
        {% block content %}
        <div class="row">
            <div class="col s12 center-align">
                <h3>Добро пожаловать!</h3>
                <h3>Сайт находится в разработке!</h3>
            </div>
        </div>
        {% endblock %}
    </div>
</main>
<!-- Site footer -->
<footer class="page-footer teal darken-2">
    <div class="container">
        <div class="footer-copyright teal darken-2">
            <!-- Site copyright -->
            <div class="row">
                <div class="col s12 center-align">
                    <span class="copyright-text">© 2020 Design by <a class="footer-link" href="https://vk.com/id152191562">Alex Parkhomov</a></span>
                </div>
            </div>
        </div>
    </div>
</footer>
{% block javascripts %}
<script type="text/javascript" src="./js/jquery-3.5.1.min.js"></script>
<script type="text/javascript" src="./js/materialize.min.js"></script>
<script type="text/javascript" src="./js/init.js"></script>
{% endblock %}
</body>
</html>